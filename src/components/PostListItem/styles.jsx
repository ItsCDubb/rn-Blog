import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 12,
    marginBottom: 12,
  },
  link: {
    color: "#0ba376",
  },
});

export default styles;
